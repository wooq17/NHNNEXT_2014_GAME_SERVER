#include "stdafx.h"
#include "DDPoint.h"

// inline DDPoint& DDPoint::operator=( const DDPoint& point )
// {
// 	this->SetPoint( point.GetX(), point.GetY() );
// 	return *this; 
// }

// inline DDPoint DDPoint::operator+( const DDPoint& point ) const
// {
// 	return DDPoint( this->m_X + point.GetX(), this->m_Y + point.GetY() );
// }

// inline DDPoint DDPoint::operator-( const DDPoint& point ) const
// {
// 	return DDPoint( this->m_X - point.GetX(), this->m_Y - point.GetY() );
// }
// 
// inline DDPoint DDPoint::operator-( ) const
// {
// 	return DDPoint( -this->m_X, -this->m_Y );
// }
// 
// inline DDPoint DDPoint::operator*( float n ) const
// {
// 	return DDPoint( this->m_X * n, this->m_Y * n );
// }
// 
// inline DDPoint DDPoint::operator/( float n ) const
// {
// 	return DDPoint( this->m_X / n, this->m_Y / n );
// }